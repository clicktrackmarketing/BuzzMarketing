import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Results & ROI",
};

export default function TestimonialsResultsRoiLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
